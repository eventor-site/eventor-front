name: CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Generate .env file and SSL files
        run: |
          cd frontend
          echo "${{ secrets.ENV_FILE }}" > .env
          echo "DOCKER_USERNAME=${{ secrets.DOCKER_USERNAME }}" >> .env

      # DockerHub 로그인
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}

      # DockerHub Push
      - name: Build & Push Docker Images (Blue & Green)
        run: |
          cd eventor-front
          docker-compose -f compose.blue-build.yml build
          docker-compose -f compose.green-build.yml build
          docker-compose -f compose.blue-build.yml push
          docker-compose -f compose.green-build.yml push

  server:
    runs-on: ubuntu-22.04
    needs: build
    steps:
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd eventor-front
            chmod +x deploy.sh
            source deploy.sh
#            sudo docker-compose stop eventor-front
#            sudo docker-compose rm -f
#            sudo docker rmi dlrudgjs104/eventor-front:latest
#            sudo docker-compose up -d